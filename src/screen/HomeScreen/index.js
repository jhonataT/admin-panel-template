import { Card } from "../../components/Card";
import { MainLayout } from "../../components/Layout";

export const HomeScreen = () => {
    return <div>
        <Card>
            home
        </Card>
    </div>
}